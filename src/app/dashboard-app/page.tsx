import { FC } from "react";

interface pageProps{}
const page:FC<pageProps> = ({}) => {
    return <div> App directory Dashboard - In app dir, we need to create dashboard folder and then page.tsx inside it.</div>
}
export default page