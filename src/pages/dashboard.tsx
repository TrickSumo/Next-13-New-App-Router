import { FC } from "react";

interface dashboardProps{}
const dashboard:FC<dashboardProps> = ({}) => {
    return <div> Dashboard - Old pages directory is still compatible for incremental adoption.</div>
}
export default dashboard